# Ejecutar una sola vez
install.packages(c(
  "rmarkdown", "knitr",
  "tidyverse",      # dplyr, ggplot2, readr, tidyr, purrr
  "haven",          # leer .sav (SPSS)
  "readxl",         # leer .xlsx
  "janitor",        # limpiar nombres de columnas
  "viridis",        # paletas de colores accesibles
  "kableExtra",     # tablas bonitas para el HTML
  "corrr",          # correlaciones
  "here"            # rutas relativas
))
